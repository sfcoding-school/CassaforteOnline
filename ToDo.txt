

create_user(mail password [pro:boolean])  DONE // non è gestito il fatto che la mail deve essere univoca 
										 // si risolve richiamando prima check_if_user_mail_exists(mail)
										 // che restituisce True se la mail già esiste o False se non è nel DB

inserisci_file(mail, dim, nome, codest) DONE 

inserisci_file(mail, dim, nome, codest) DONE 

check_space(owner, dim_NewFile):Boolean DONE

check_if_user_mail_exists (mail) DONE

user_login(mail,pwd):boolean DONE

get_file(codest, mail):id_file DONE nomefile  

get_file_list(mail):codest DONE  tutti i file della mail Restituisce una lista di  dizionari Treccani

make_user_pro(mail): DONE restituisce 1 in ogni caso eppure solleva eccezione

set_file_available: DONE restituisce 1 se tutto ok, altrimenti lancia eccezione

-------------------------------------------------------------


registrazione / login
	sessione PHP
upload pro PHP/py
lista pro PHP/py


Download File
	normale/pro
	PHP/py

upload sistema Luca
 
